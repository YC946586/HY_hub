<UserControl x:Class="HY_Main.View.HomePage.UserControls.BottomContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HY_Main.View.HomePage.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical"  Margin="20 0">
            <StackPanel Orientation="Horizontal" Margin="0 15">
                <TextBlock  Text="&#xe6af;" FontFamily="{DynamicResource IconFont}"     VerticalAlignment="Center"   
                                   FontSize="20" Foreground="#333333"  Margin="0 0 10 0" />
                <TextBlock Text="热门下载" FontSize="20" FontWeight="Black" Foreground="#333333"/>
            </StackPanel>
          
            <ItemsControl ItemsSource="{Binding Path=DownloadManager.Groups}" Background="#F2F2F2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <Border BorderThickness="0 0 0 2" BorderBrush="#797979"  Margin="10 0">
                                <Grid Margin="0 10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="排名" Width="60"   HorizontalAlignment="Center"/>
                                    <TextBlock Text="游戏" Grid.Column="1"/>
                                </Grid>
                            </Border>
                            <ItemsControl ItemsSource="{Binding MenuModels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="10 10 10 0"  VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Sort}"/>
                                            <TextBlock Text="{Binding GameName}" Grid.Column="1"/>
                                            <Button Content="获取游戏" Margin=" 20 0 " HorizontalAlignment="Right"  Style="{StaticResource ButtonPrimary}" Grid.Column="2"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                      
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        <Grid  Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 15">
                    <TextBlock  Text="&#xe6af;" FontFamily="{DynamicResource IconFont}"     VerticalAlignment="Center"   
                                   FontSize="20" Foreground="#333333"  Margin="0 0 10 0" />
                    <TextBlock Text="游戏工具" FontSize="20" FontWeight="Black" Foreground="#333333" />
                </StackPanel>
                <StackPanel Background="White"  Orientation="Vertical" >
                    <TextBlock Text="安装游戏必备运行库，可解决99%游戏无法进入的问题。" Foreground="#909090" Margin="10 10 10 0"  TextWrapping="Wrap" FontSize="15"/>
                    <TextBlock  Text="&#xe6a7;" FontFamily="{DynamicResource IconFont}"   HorizontalAlignment="Right" Margin="10"  VerticalAlignment="Center"   
                                   FontSize="20" Foreground="#A8A8A8"  />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock  Text="&#xe6af;" FontFamily="{DynamicResource IconFont}"     VerticalAlignment="Center"   
                                   FontSize="20" Foreground="#333333"  Margin="0 0 10 0" />
                    <TextBlock Text="关于黑鹰 hub" FontSize="20" FontWeight="Black" Foreground="#333333" Margin="0 15"/>
                </StackPanel>
            </StackPanel>
            <Border Background="White"  Grid.Row="1">
                <TextBlock Text="黑鹰hub介绍"  Foreground="#909090" Margin="10" TextAlignment="Center" HorizontalAlignment="Center"  VerticalAlignment="Center" TextWrapping="Wrap" FontSize="15"/>
            </Border>

        </Grid>
    </Grid>
</UserControl>
